# Code owners for cc-gateway
#
# Configure branch protection on `main` to require Code Owner reviews. This
# file defines ownership, but does not enforce merge restrictions by itself.

# Global fallback used when Code Owner review protection is enabled.
*                 @Loveyless

# Most sensitive paths — CI/signing/release and the Rust backend. Listed
# explicitly so they stay locked even if the global rule above is relaxed later.
/.github/         @Loveyless
/src-tauri/       @Loveyless
